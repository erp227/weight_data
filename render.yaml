
services:
  - type: web
    name: weight-monitor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: WEIGHT_FILE_PATH
        value: /opt/render/project/src/weight.txt
      - key: PYTHON_VERSION
        value: 3.11.0
